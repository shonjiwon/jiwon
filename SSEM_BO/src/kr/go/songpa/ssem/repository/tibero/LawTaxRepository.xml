<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.go.songpa.ssem.repository.tibero.LawTaxRepository">

    <select id="selectLawTaxList" parameterType="kr.go.songpa.ssem.model.SearchVo" resultType="kr.go.songpa.ssem.model.LawtaxCounselVo">
        SELECT * FROM (
        SELECT ROWNUM AS RNUM, T.* FROM (
        SELECT A.* FROM LAWTAX_COUNSEL A
        WHERE
        1=1
        <if test="searchKind != null and searchKind !=''">
            AND A.GBN = #{searchKind}
        </if>
        <if test="searchKind2 != null and searchKind2 !=''">
            AND A.STATUS = #{searchKind2}
        </if>
        <if test="searchKind3 != null  and searchKind3 !=''">
            <choose>
                <when test="searchKind3 == '1'.toString()">
                    AND ( TO_CHAR(A.REG_DT, 'YYYY-MM-DD') BETWEEN #{searchSDate} AND #{searchEDate} )
                </when>
                <when test="searchKind3 == '2'.toString()">
                    AND ( TO_CHAR(A.UP_DT, 'YYYY-MM-DD') BETWEEN #{searchSDate} AND #{searchEDate} )
                </when>
                <when test="searchKind3 == 'ALL'.toString()">
                    AND ( TO_CHAR(A.UP_DT, 'YYYY-MM-DD') BETWEEN #{searchSDate} AND #{searchEDate} OR TO_CHAR(A.REG_DT, 'YYYY-MM-DD') BETWEEN #{searchSDate} AND #{searchEDate})
                </when>
            </choose>
        </if>
        <if test="searchVal != null  and searchVal !=''">
            <choose>
                <when test="searchKind4 == '1'.toString()">
                    AND (UPPER(A.TITLE) like '%'||UPPER(#{searchVal})||'%')
                </when>
                <when test="searchKind4 == '2'.toString()">
                    AND (UPPER(A.APY_NM) like '%'||UPPER(#{searchVal})||'%')
                </when>
                <when test="searchKind4 == '3'.toString()">
                    AND (UPPER(A.APY_ID) like '%'||UPPER(#{searchVal})||'%')
                </when>
                <when test="searchKind4 == 'ALL'.toString()">
                    AND ( UPPER(A.APY_ID) like '%'||UPPER(#{searchVal})||'%' OR UPPER(A.APY_NM) like '%'||UPPER(#{searchVal})||'%' OR UPPER(A.TITLE) like '%'||UPPER(#{searchVal})||'%' )
                </when>
            </choose>
        </if>
        ORDER BY A.REG_DT DESC
        ) T WHERE ROWNUM &lt;=#{lastIndex}
        ) WHERE RNUM &gt;=#{firstIndex}
    </select>

    <select id="selectLawTaxListTotal" parameterType="kr.go.songpa.ssem.model.SearchVo" resultType="int">
        SELECT COUNT(*) FROM LAWTAX_COUNSEL A
        WHERE
        1=1
        <if test="searchKind != null and searchKind !=''">
            AND A.GBN = #{searchKind}
        </if>
        <if test="searchKind2 != null and searchKind2 !=''">
            AND A.STATUS = #{searchKind2}
        </if>
        <if test="searchKind3 != null  and searchKind3 !=''">
            <choose>
                <when test="searchKind3 == '1'.toString()">
                    AND ( TO_CHAR(A.REG_DT, 'YYYY-MM-DD') BETWEEN #{searchSDate} AND #{searchEDate} )
                </when>
                <when test="searchKind3 == '2'.toString()">
                    AND ( TO_CHAR(A.UP_DT, 'YYYY-MM-DD') BETWEEN #{searchSDate} AND #{searchEDate} )
                </when>
                <when test="searchKind3 == 'ALL'.toString()">
                    AND ( TO_CHAR(A.UP_DT, 'YYYY-MM-DD') BETWEEN #{searchSDate} AND #{searchEDate} OR TO_CHAR(A.REG_DT, 'YYYY-MM-DD') BETWEEN #{searchSDate} AND #{searchEDate})
                </when>
            </choose>
        </if>
        <if test="searchVal != null  and searchVal !=''">
            <choose>
                <when test="searchKind4 == '1'.toString()">
                    AND (UPPER(A.TITLE) like '%'||UPPER(#{searchVal})||'%')
                </when>
                <when test="searchKind4 == '2'.toString()">
                    AND (UPPER(A.APY_NM) like '%'||UPPER(#{searchVal})||'%')
                </when>
                <when test="searchKind4 == '3'.toString()">
                    AND (UPPER(A.APY_ID) like '%'||UPPER(#{searchVal})||'%')
                </when>
                <when test="searchKind4 == 'ALL'.toString()">
                    AND ( UPPER(A.APY_ID) like '%'||UPPER(#{searchVal})||'%' OR UPPER(A.APY_NM) like '%'||UPPER(#{searchVal})||'%' OR UPPER(A.TITLE) like '%'||UPPER(#{searchVal})||'%' )
                </when>
            </choose>
        </if>
    </select>

    <select id="selectLawTax" parameterType="kr.go.songpa.ssem.model.LawtaxCounselVo" resultType="kr.go.songpa.ssem.model.LawtaxCounselVo">
        SELECT * FROM LAWTAX_COUNSEL WHERE LAWTAX_IDX = #{lawtax_idx}
    </select>

    <update id="updateLawTax" parameterType="kr.go.songpa.ssem.model.LawtaxCounselVo">
		UPDATE LAWTAX_COUNSEL SET
            STATUS = #{status}
			, RPY_CONT = #{rpy_cont}
			, UP_ID = #{up_id}
			, UP_DT = SYSDATE
		WHERE LAWTAX_IDX = #{lawtax_idx}
	</update>

    <delete id="deleteLawTax" parameterType="kr.go.songpa.ssem.model.LawtaxCounselVo">
        DELETE FROM LAWTAX_COUNSEL WHERE LAWTAX_IDX = #{lawtax_idx}
    </delete>

</mapper>
